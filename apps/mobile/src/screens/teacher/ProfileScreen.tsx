// ProfileScreen is used as a redirect — the actual profile is TeacherProfileScreen
// This re-exports to prevent import errors if referenced
export { TeacherProfileScreen as ProfileScreen } from './TeacherProfileScreen';
